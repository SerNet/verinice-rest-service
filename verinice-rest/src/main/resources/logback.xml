<?xml version="1.0" encoding="UTF-8"?>
<configuration>

  <!-- <statusListener class="ch.qos.logback.core.status.NopStatusListener" /> -->
  <appender
    name="STDOUT"
    class="ch.qos.logback.core.ConsoleAppender">
    <layout class="ch.qos.logback.classic.PatternLayout">
      <Pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</Pattern>
    </layout>
  </appender>

  <appender
    name="FILE"
    class="ch.qos.logback.core.rolling.RollingFileAppender">
    <file>./verinice-rest.log</file>
    <encoder class="ch.qos.logback.classic.encoder.PatternLayoutEncoder">
      <Pattern>%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n</Pattern>
    </encoder>

    <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
      <!-- rollover daily -->
      <fileNamePattern>./verinice-rest.log.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
      <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
        <maxFileSize>5MB</maxFileSize>
      </timeBasedFileNamingAndTriggeringPolicy>
    </rollingPolicy>
  </appender>

  <!-- Set na individual log level for packages or classes here -->
  <!-- <logger name="org.verinice.service" level="debug" /> -->

  <root level="warn">
    <!-- You can activate multiple appenders -->
    <appender-ref ref="FILE" />
    <!-- Activate this appender to log to standart out -->
    <appender-ref ref="STDOUT" />
  </root>

</configuration>
